/**
 * @deprecated Use `@/lib/types` for `Folder` and `FlashCard`, and `@/lib/shuffle` for shuffle.
 * Kept for backwards compatibility with any external imports.
 */
export type { FlashCard, Folder } from "@/lib/types"
export { shuffleArray } from "@/lib/shuffle"
